window.onload=function(){
    let movePic=document.querySelector(".movePic");
    let include=document.querySelector(".include")
    let kuan=include.offsetWidth;  
    let moveImg=document.querySelectorAll(".moveImg");
    let width=moveImg[0].offsetWidth;
    let t=setInterval(move,1000)
    function move(){
        let now=moveImg[0].style.left=0;
        let num=0;
        for(let i=0;i<moveImg.length;i++){
            for(let j=0;j<move.length;j++){
                
            }
        }
    }
    
    
}